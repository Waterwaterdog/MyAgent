# 面试防守材料 (Interview Defense)

本项目是一个独立设计的 Coding Agent 系统。以下是针对核心设计决策的 15 个关键问题及回答，旨在展示对 Agent 运行机制的深度理解。

### Q1: 为什么不用 LangChain？
**回答**：为了深刻理解并自主把控 Agent 的核心循环。LangChain 等框架虽然方便，但其高度抽象掩盖了 LLM 交互的底层逻辑（如 Prompt 组装、Tool 调用解析、上下文迭代）。独立实现能让我们更精细地优化 KV Cache 复用、实现自定义的防死循环机制以及更鲁棒的错误处理闭环，这对于推免考核中展示技术深度至关重要。

### Q2: 你的 Agent Loop 是什么？
**回答**：我们的 Agent Loop 采用了 `Decision -> Action -> Observation -> Thinking` 的迭代模式。在 `Runtime` 模块中实现，支持三种模式：
1. **Standard**: 开放式循环。
2. **ReAct**: 显式的“推理-行动-观察”循环。
3. **Planning**: 先生成结构化计划，再针对每个步骤执行微循环。

### Q3: LLM 如何决定调用哪个 Tool？
**回答**：通过大模型的原生 **Tool Calling (Function Calling)** 能力。我们在 Prompt 中注入了工具的 JSON Schema（包含名称、描述和参数定义）。模型根据任务意图输出特定格式的调用指令，`Runtime` 模块捕获并解析该指令，然后分发给 `ToolRegistry` 执行对应的本地函数。

### Q4: Tool Schema 为什么需要独立定义？
**回答**：为了实现**关注点分离**和**文档动态注入**。独立定义 Schema 使得工具可以自描述。这支持了我们的“API 摘要”策略：默认给模型精简的摘要以节省 Token，只有当模型调用出错时，才从 Schema 中提取完整文档进行动态补全，实现按需学习。

### Q5: Tool Factory 解决了什么问题？
**回答**：解决了**扩展性**和**耦合度**问题。通过 `ToolFactory` 和 `ToolRegistry`，新增工具只需继承 `BaseTool` 并注册即可，无需修改 `Agent` 或 `Runtime` 的核心代码。这使得工具系统成为了一个可插拔的插件系统。

### Q6: 为什么有些 Tool 可以并行，有些必须串行？
**回答**：这涉及**副作用 (Side Effects)** 和 **数据依赖**。读取类工具（如 `read_file`）通常是幂等的且互不干扰，可以并行以提升效率；而写操作或命令执行（如 `write_file`, `run_command`）可能修改环境状态或存在前后依赖，必须串行执行以保证逻辑一致性和环境稳定性。

### Q7: Tool 报错以后发生什么？
**回答**：系统不会崩溃，而是触发 **Error -> Model 决策闭环**。我们将原始异常包装为标准化的 `AgentError` 对象（包含错误码、类型、建议操作），将其反馈给模型。模型在下一轮对话中会感知到错误，并根据“建议操作”自主决定是修正参数重试，还是切换策略（如发现文件不存在后改用 `list_dir`）。

### Q8: 如何防止 Agent 无限循环？
**回答**：我们实现了**五层防护机制**：
1. `MAX_STEPS`: 硬性的最大步数限制。
2. `Repeated Call Guard`: 监测完全相同的工具调用参数，连续重复则拦截。
3. `No Progress Detection`: 监测环境状态（如文件变化），无进展时触发预警。
4. `Global Timeout`: 任务执行的总时间限制。
5. `LLM Feedback`: 在检测到潜在循环时，向模型发送系统提醒要求其改变策略。

### Q9: Plan 和 ReAct 有什么区别？
**回答**：**Plan** 关注“做什么 (What)”，是战略层面的任务分解，适合长链条、复杂任务；**ReAct** 关注“怎么做 (How)”，是战术层面的动态调整。我们的混合架构（Plan + ReAct）利用 Planner 生成大局观，在执行每个步骤时利用 ReAct 保持对环境变化的敏感度。

### Q10: 为什么需要 Context Compression？
**回答**：LLM 的上下文窗口是有限且昂贵的。随着对话增长，Token 消耗会迅速增加。`Context Compression` 允许系统在接近预算阈值时，利用 LLM 对旧的对话和执行结果进行**语义总结**，只保留关键信息和最近的原始上下文，从而支持超长链条的任务。

### Q11: Tool Result 为什么不能全部放进上下文？
**回答**：因为工具输出可能包含海量无关数据（如几万行日志）。这不仅会撑爆上下文窗口，还会引入大量噪音干扰模型的判断。我们的 `ResultAnalyzer` 会对超长输出进行智能截断和摘要提取，只把核心结果反馈给模型，而将完整数据保留在 Trace 系统中供人类查阅。

### Q12: 你的 Memory 分成哪几层？为什么？
**回答**：分为三层，实现了**知识生命周期管理**：
1. **短期 (Short-term)**: 即时对话，随压缩而更新。
2. **中期 (Mid-term)**: 会话内 insights（项目规范、已证实的策略），在整个 Session 内保持稳定。
3. **长期 (Long-term)**: 跨会话的持久化知识（用户偏好、通用经验），存储在磁盘。

### Q13: Prompt 为什么要分静态和动态？
**回答**：为了优化 **KV Cache 复用**。静态部分（身份定义、规则）放在 Prompt 起始位置且保持恒定；动态部分（任务、计划、历史）放在后面。这使得模型服务端可以缓存静态部分的计算结果，显著降低首字延迟并减少计算成本。

### Q14: Trace 如何帮助你 Debug？
**回答**：Trace 系统为每次运行生成唯一 ID，记录了从任务启动到结束的每一个关键事件（模型输入输出、工具参数结果、延迟、错误码）。这使得我们可以**精确还原** Agent 的决策链路，快速定位是模型指令错误、工具实现 Bug 还是环境问题，实现了全链路的可解释性。

### Q15: 为什么这些设计是你自己的 Agent，而不是套了一个 Agent 框架？
**回答**：因为整个 **Runtime 执行引擎**、**上下文分层管理逻辑**、**分层记忆系统** 以及 **全链路追踪体系** 都是我们从零构建的。我们直接与模型原生的 Tool Calling 接口通信，自主管理每一轮的 Context 组装和状态迭代，而不是调用 LangChain 等框架的 `AgentExecutor`。这证明了我们对 Agent 核心机制的掌控力。
