# 演示指南 (Demo Guide)

本指南旨在指导如何录制一个 2 分钟以内的专业演示视频，充分展示 Coding Agent 的核心竞争力和工程亮点。

## 1. 演示任务设计 (Recommended Task)

**任务描述**：
> "项目根目录下有一个名为 `math_utils.py` 的文件，其中 `add` 函数的实现似乎有误（可能是为了演示故意留下的 Bug）。请分析该文件，编写并运行一个测试文件 `test_math_utils.py` 来复现错误，然后修复 Bug 并再次运行测试以验证修复。"

**为什么选择这个任务？**
- 展示了 **Plan + ReAct 混合模式**：Agent 会先规划步骤。
- 展示了 **自主读写能力**：读取代码、编写测试。
- 展示了 **错误处理与自治愈**：测试失败后，模型需要分析错误原因并修正代码。
- 展示了 **全链路 Trace**：运行结束后查看生成的 JSON 日志。

## 2. 演示步骤与脚本

### 准备工作 (0:00 - 0:20)
1. 确保根目录下有 `math_utils.py`，内容如下：
   ```python
   def add(a, b):
       return a - b  # 故意写错的 Bug
   ```
2. 打开终端，展示项目结构。

### 启动 Agent (0:20 - 0:40)
使用混合模式启动：
```bash
python -m coding_agent.main --hybrid "分析 math_utils.py 中的 add 函数，编写测试文件 test_math_utils.py 复现错误并修复它。"
```
- **解说点**：强调正在使用 `Hybrid Mode`，Agent 首先生成了一个多步计划。

### 执行与思考 (0:40 - 1:20)
观察 Agent 的输出：
1. **规划**：Agent 输出 JSON 格式的计划。
2. **执行**：Agent 调用 `read_file` 查看代码。
3. **行动**：Agent 调用 `write_file` 创建测试。
4. **报错**：Agent 调用 `run_command` 运行测试，发现 `1 + 1` 变成了 `0`（报错）。
5. **修复**：Agent 捕获错误，解释原因（减号误用），调用 `edit_file` 或 `write_file` 修复代码。
6. **验证**：Agent 再次运行测试，显示 `Success`。

### 特色功能展示 (1:20 - 1:50)
1. **Trace 日志**：打开 `logs/traces/` 下刚生成的 JSON 文件，展示结构化的调用链。
2. **Memory 效果**：简要说明 Agent 如何记住了之前的错误并避免再次犯错。
3. **压缩机制**（可选）：如果对话较长，可以提到 Token 自动管理。

### 总结 (1:50 - 2:00)
- 强调：核心循环自主实现、零框架依赖、完善的工程化防护与追踪。

## 3. 视频录制建议
- **录屏软件**：推荐使用 OBS 或系统自带录屏。
- **分辨率**：1080p 或 720p，保证文字清晰。
- **大小限制**：确保最终视频文件小于 200MB。
- **背景音**：建议真人配音讲解，语气从容自信。
